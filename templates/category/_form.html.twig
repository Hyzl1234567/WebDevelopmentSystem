{{ form_start(form, {'attr': {'id': 'category_form'}}) }} 
 {{ form_start(form, {'attr': {'id': 'update-form'}}) }}
    {{ form_row(form.name) }}
    {{ form_row(form.description) }}
{{ form_end(form) }}
